# Runtime dependencies for the standalone Bio Pipeline MCP server.
# Mirrors pyproject.toml; use `pip install -r requirements.txt` for a plain
# (non-package) install, or `pip install .` to also get the console script.
mcp>=1.2
httpx>=0.27
